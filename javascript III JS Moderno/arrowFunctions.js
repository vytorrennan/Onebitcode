const sum = (a, b) => {
    return a + b;
}

const multiply = (a, b) => a * b //when it's one line only you don't need the "return"

const double = n => `${n} times 2 is ${n * 2}`; // one there's just one parameter you don't even need the parentheses
